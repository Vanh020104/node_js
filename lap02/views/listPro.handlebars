<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>home</title>
</head>
<body>
  <header>
    <div class="container">
      <img src="/img/logo.png" alt="Logo" class="logo">
      <nav>
        <ul>
          <li><a href="/">Home</a></li>
          <li><a href="/about">About</a></li>
          <li><a href="/listPro">Product</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <main>
  <h2 style="margin: 30px 0;text-align: center;">List products</h2>
  <div class="products-container">
    {{#each  products}}
    <div>
      <h2>{{name}}</h2>
      <img src="{{image}}" alt="{{name}}">
    </div>
    {{/each}}
  </div>
</main>


</body>
<style>
    /* Định dạng container chứa các sản phẩm */
.products-container {
  display: flex;
  flex-wrap: wrap;
  justify-content: center; /* căn giữa các sản phẩm */
  gap: 20px; /* Khoảng cách giữa các sản phẩm */
}

/* Định dạng mỗi sản phẩm */
.product {
  flex: 0 0 calc(33.33% - 20px); /* 33.33% để hiển thị 3 sản phẩm trên 1 hàng */
  text-align: center; /* căn giữa nội dung trong sản phẩm */
}

/* Định dạng ảnh sản phẩm */
.product img {
  max-width: 100%; /* Đảm bảo ảnh không vượt quá kích thước của phần tử cha */
}

/* Định dạng tên sản phẩm */
.product span {
  display: block; /* Hiển thị tên sản phẩm trên một dòng mới */
  margin-top: 10px; /* Khoảng cách giữa ảnh và tên sản phẩm */
}

    /* Reset CSS để loại bỏ các giá trị mặc định */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* Định dạng phần header */
header {
  background-color: #f2f2f2;
  border-bottom: 1px solid #ccc;
}

/* Container để căn giữa */
.container {
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 20px;
}

/* Định dạng logo */
.logo {
  width: 100px; /* Điều chỉnh kích thước của logo */
  height: auto;
}

/* Định dạng menu điều hướng */
nav ul {
  list-style: none;
  display: flex;
}

nav ul li {
  margin-right: 20px;
}

nav ul li a {
  text-decoration: none;
  color: #333;
  font-weight: bold;
}

nav ul li a:hover {
  color: #555;
}

</style>
</html>
